# FlyOhtani 구현 작업 안내

목표는 실제 초파리 크기의 몸과 실제 연결망에서 유도한 회로로, 눈에 보이는
공을 배트로 쳐내는 것을 학습시키고, 그 과정(회로 구조·시각 입력·스파이크)을
시뮬레이션으로 관찰하는 것이다.

## 읽는 순서

필독은 이 파일 + 아래 2개뿐이다:

1. `docs/PLAN.md` — 결정의 단일 기준. 설계 결정(D01~D24), Phase와 게이트
2. `docs/records/STATUS.md` — 지금 검증된 것과 막혀 있는 것

그 다음, **맡은 작업에 해당할 때만** 읽는다:

- 몸·접촉·시각 작업 → `docs/records/PRIOR-FINDINGS.md`
  (v1에서 확정된 사실과 반복하면 안 되는 실패)
- 외부 데이터/코드 반입 → `docs/RESEARCH_SOURCES.md`

문서는 5개가 전부다. 늘리기 전에 기존 문서에 들어갈 자리가 없는지 본다.

## 역할과 작업 원칙

- 사용자와 Codex는 연구 계획·구조·실험 규약을 정리한다. 사용자와 Claude는
  실제 구현·테스트를 진행한다.
- 사용자가 지정한 작업 단위를 구현한다. 독립적인 기반 작업은 미결정 연구
  항목 때문에 멈추지 않는다.
- **미결정 연구 선택을 임의의 수치로 채우지 않는다.** 의존하는 작업만
  대기하고 독립 작업을 계속한다. 값을 정해야 한다면 그것이 공학적 선택임을
  명시하고 근거를 남긴다.
- 설치 성공, 물리 검증, 학습 성능을 가정하지 않는다. 실행해서 확인한다.
- **테스트 통과와 연구 가설의 검증을 구분한다.** 실행 결과는
  `docs/records/VALIDATION_LOG.md`, 상태 변화는 `docs/records/STATUS.md`.
- 실패·무효 결과를 보존한다. 결과가 나쁘다고 시드를 제외하거나 과거 실행을
  삭제하지 않는다.
- 연구 설계 변경은 `docs/PLAN.md`에 반영한다. 날짜·변경 전후·이유·이미
  열람한 결과 범위를 남긴다.
- 외부 코드·데이터의 출처·버전·체크섬·라이선스를
  `docs/RESEARCH_SOURCES.md`와 데이터 manifest에 기록한다.
- 실험과 무관한 폴더 이동·전체 재작성보다 책임 분리와 필요한 변경을
  우선한다.

## 게이트

Phase는 게이트를 통과해야 다음으로 넘어간다. 기준은 **실행 전에** 고정하고,
결과가 나쁘다고 완화하지 않는다.

| 게이트 | 질문 | 상태 |
| --- | --- | --- |
| G1 | 파리 앞다리가 배트를 실제로 휘두를 수 있는가 (무공 스윙 스윕) | 미실행 |
| G2 | 접촉이 사전 등록 수용치를 통과하는가 | v1에서 **FAIL**, 새 스케일 미실행 |
| G3 | 학습을 시작해도 되는 상태인가 (누출·exploit·예산) | 미실행 |
| G4 | MaleCNS 루밍 서브그래프를 실제로 받을 수 있는가 | 미확인 |

**G2를 통과하기 전에는 학습을 시작하지 않는다.**

## 다음 작업

`docs/records/STATUS.md`의 "다음 작업" 절을 따른다. 완료된 작업의 지시를
이 파일에 쌓지 않는다 — v1에서 이미 끝난 일이 최신 작업처럼 보이는 문제를
만들었다.

## 이전 버전

v1(사람 크기 구장 + 3축 강체 배트 + 인간식 스윙 협응 탐색) 전체는 git 태그
`archive/human-scale-v0`에 있다. 그 접근으로 돌아가지 않는다.

이 파일은 인계 안내이며 현재 사용자 지시가 우선한다.
