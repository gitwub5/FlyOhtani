# 현재 상태

2026-09-17 갱신 (Phase 0 완료, **G1 PASS**, **G4 PASS**). 이 문서는 **지금**
무엇이 검증됐고 무엇이 막혀 있는지만 담는다. 결정은 [PLAN](../PLAN.md),
v1에서 넘어온 사실은 [선행 발견](PRIOR-FINDINGS.md), 실행 근거는
[검증 기록](VALIDATION_LOG.md).

## 통과한 게이트

### G1 — 파리 앞다리가 배트를 휘두를 수 있는가: **PASS (조건부)**

[전체 보고](G1-FORELEG-SWING.md). 원본 NeuroMechFly MJCF에서 실제 관절·질량·
메시 그대로 오른쪽 앞다리를 추출해 배트를 달고 240런을 돌렸다. 전부 안정,
dt 수렴 0.07%, 세 적분기 편차 0.02%. 도달거리 1.81 mm.

**문자 그대로의 질문은 통과다 — 구동력 병목은 없다.** 그러나 액추에이터가
전체 시간의 약 5%만 포화하므로 **측정된 속도는 파리의 능력 상한이 아니라
`kp`와 명령한 목표각이 만든 값**이다. 둘 다 자유 파라미터다. Phase 2로
넘기는 것은 단일 "최대 속도"가 아니라 부하 대 속도의 **관계**다.

### G4 — MaleCNS 루밍 회로를 받을 수 있는가: **PASS**

[전체 보고](G4-CONNECTOME-ACCESS.md). MaleCNS v1.0, CC-BY(상업적 이용 허용).
LC4(126) / LPLC2(185) → 하강뉴런 12개, 간선 1,343, 시냅스 39,549의 부분회로를
추출해 저장소에 넣었다(49 KB, provenance 포함). LC4→DNp04(11,597),
LPLC2→DNp01 Giant Fiber(4,862) 등 문헌의 루밍-탈출 경로가 데이터에 그대로
있다. **배선을 받은 것이며 기능을 검증한 것이 아니다.**

## 지금 실제로 동작하는 것

| 대상 | 상태 |
| --- | --- |
| `flyohtani/units.py` | SI 환산이 v1 검증값과 일치 |
| `flyohtani/body/` | 원본 MJCF에서 앞다리 빌드, 배트 부착, G1 스윕 |
| `flyohtani/brain/connectome.py` | 루밍 부분회로 로더(오프라인) + 재추출기 |
| `flyohtani/sense/` | 합성 프레임 검출·추적. **렌더 기반 검증은 여전히 불가** |
| 입력 누출 방지 | AST 정적 검사 통과 |
| 테스트·린트 | 두 진입점 모두 **54 passed**, `ruff check .` 클린 |

## 알려진 한계 (임의로 고치지 않고 그대로 보고)

- **이 몸은 실제 파리 DOF를 5개 갖는다 — 7개가 아니다.** 사지축 roll DOF 2개는
  관성이 ~1000배 작아(원본 실측 2.914e-08 / 1.784e-08 g·mm²) flygym 자신의
  기본 dt로도 구동할 수 없다. 잠갔고, 근거를 회귀 테스트로 고정했다.
- **G1은 낙관적 측정이다.** 손목이 강체다(수동 tarsus 컴플라이언스 제거).
  실패였다면 결론이었겠지만 통과이므로 결론이 아니다 — Phase 2에서 재측정.
- **관절 각속도에 생물학적 근거가 없다.** 측정된 280~2,184 rad/s(최대 초당
  268회전)를 정당화할 문헌을 아직 확보하지 않았다. **Phase 2의 선결 과제.**
- **`boundmass`가 결과를 8.3% 바꾼다.** 격자의 최경량 배트(5e-7 g)는 오염됐다.
  사용 가능한 질량 하한은 실질적으로 1e-6 g.
- **몸통은 world 용접, 머리는 고정.** 균형·지면반력·목 관절 없음. 공을 쳐도
  반작용으로 밀리지 않는다.
- **렌더 기반 시각 검증이 비어 있다.** 이제 렌더할 몸은 생겼지만 세계(구장·
  공)가 없다. Phase 2/3에서 복원하기 전까지 검출률을 보고하지 않는다.
- **`VisionObservation`의 자기감각 필드가 죽은 이름이다**(v1의 torso/swing/
  tilt). Phase 3에서 앞다리 관절로 교체.
- **`BallDetector`의 공 색이 v1 씬 재질값이다.** Phase 2의 새 공과 맞춰야 한다.
- **접촉 수용 기준 미달이 미해결로 넘어왔다**(v1 반발계수 ≈0.29, 침투
  0.04~0.05 m). **게이트 G2 통과 전에는 학습을 시작하지 않는다.**
- **회로의 입력이 없다.** 망막→LC 경로, 신경전달물질 부호, 운동 디코딩 모두
  미결정.

## 다음 작업

**Phase 2(세계와 접촉)**. 선결 과제 둘:

1. 실제 초파리 다리의 방어 가능한 각속도/일률 상한을 문헌에서 확보한다.
   이것 없이는 공 질량·투구 속도·구장 치수를 정할 수 없다(G1 §4).
2. 게이트 G2 — [선행 발견](PRIOR-FINDINGS.md) §2의 사전 등록 수용치를 새
   스케일에서 재실행하고 solref/solimp 후보를 비교한다.

Phase 3(망막 스파이크 인코딩)은 Phase 2의 세계가 있어야 렌더 검증이 되므로
G2 이후다. Phase 4의 LIF 회로는 입력 명세가 정해지면 착수 가능하다.
