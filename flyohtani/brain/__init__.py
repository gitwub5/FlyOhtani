"""Phase 4: the connectome-derived circuit.

`connectome` holds the real MaleCNS looming subgraph (wiring only). The LIF
simulation, the synapse-count-to-weight assumption, the plasticity rule and
the motor decoding do not exist yet -- see docs/PLAN.md.
"""
