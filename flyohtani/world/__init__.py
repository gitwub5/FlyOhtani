"""Phase 2: the scaled ballpark, the pitch, and ball-bat contact.

`batter` builds the scene, `park` is the scenery around it, `swing` steps a
swing and describes the batted ball, `rollout` runs a whole episode fast, and
`contact` holds the contact parameters G2 selected.

The measurements that chose those parameters live in `flyohtani.studies`.
"""
